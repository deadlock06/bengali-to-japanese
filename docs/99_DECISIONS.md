# 99 DECISIONS LOG — Why Things Are The Way They Are
<!-- READ WHEN: about to change/question an existing design, or logging a new decision. APPEND-ONLY. ~1K tokens -->
<!-- FORMAT: D-NNN | date | decision | reason | supersedes -->

**D-001 | 2026-07 | All v4.0 coercion mechanics permanently removed.** Banned concepts (grep list): "dopamine engine", "forced output", "speak or die", "loop never ends", hidden skip, session lock, screen lock, ghost streaks, streak saves, loot drops, mystery boxes, variable rewards, "subconscious triggers", social-comparison pressure, guilt copy. Reason: autonomy violations, app-store + consumer-law risk, harm to vulnerable users. Any stale reference to these in older docs is void. Supersedes v4.0 entirely and stray v4.1 remnants (Flow-state hidden skip, "cannot bypass" cap, locked break screen, disabled Back button).

**D-002 | 2026-07 | Speaking scored by forced alignment against known targets, never open transcription.** Reason: whisper base cannot reliably openly transcribe Bengali-accented learner Japanese in noisy rooms; alignment against the expected sentence is a far easier, robust problem. STT spike (T-000a) gates the pillar. Text-input fallback always available.

**D-003 | 2026-07 | FSRS mood adaptation moved from rating math to card selection.** Old code multiplied the 1–4 rating by a mood factor → invalid ratings (e.g. 6) fed into parameters fitted on real review data → corrupted scheduling. New design: rating stays pure; mood adjusts queue size and easy-first ordering (08). `emotional_difficulty` column dropped (06).

**D-004 | 2026-07 | Confusion/burnout/confidence detection is deterministic (agents), never LLM-judged.** Reason: a 1.7B model can't reliably estimate confidence or detect confusion; taps/timing/accuracy signals can. LLM system prompt trimmed accordingly (08).

**D-005 | 2026-07 | Voice-stress anxiety detection deferred to post-MVP.** Reason: unproven on-device, privacy-sensitive, and the tap/error signals already cover the need.

**D-006 | 2026-07 | Content factory budget corrected: $12–18K MVP (5K pairs + 2K audio), $40–60K to 50K pairs + 10K audio.** Reason: original $15–25K under-priced scarce Bengali-Japanese bilingual expert review and omitted audio production costs.

**D-007 | 2026-07 | Cloud LLM pricing corrected to per-million tokens; offline justified by latency/reliability, not cost.** Reason: original math was ~1000× off; the cost argument collapsed, but the product argument (instant, deterministic, works with zero connectivity) is stronger anyway. All decks/docs use this framing.

**D-008 | 2026-07 | Monolithic 1.7GB app replaced by 45MB base APK + tiered signed packs with P2P sharing.** Reason: target users have <200MB/mo data; a 1.7GB install was existentially incompatible with the market. See 03. LLM (Tier 3) is optional by design because retrieval covers all graded/explained content.

**D-009 | 2026-07 | Daily cap and breaks are recommendations (parental mode is the only firm-cap exception).** Resolves the v4.1 contradiction between Principles 7–8 ("cannot bypass", "screen locks") and the agent/UI specs ("recommend, never force"). Policy in 01 §Session-health governs.

**D-010 | open | Firebase vs Supabase.** To be decided at T-105. Criteria: Bangladesh latency, pricing at 100K MAU, offline SDK quality, data-residency posture. Log outcome here.

**D-011 | 2026-07-09 | Kana stroke-order data sourced from KanjiVG, not kana-svg-data.** The old `tools/fetch_stroke_data.mjs` pulled `kana-svg-data` medians, which split self-crossing (loop) strokes into two paths → 16/92 kana had wrong stroke counts (あ→4, ヲ under-counted 2-vs-3, etc.). Shipping that would teach incorrect stroke order (violates 00 §4 correctness-over-generation). New tool fetches KanjiVG (canonical stroke order, one `<path>` per stroke), flattens each stroke path to sampled median points, and scales the 109 viewBox to the consumer's 0..1000 y-down space — output JSON contract and `writing_screen.dart` unchanged. Validated: 0/92 count mismatches vs the canonical gojūon table. License note: KanjiVG is **CC BY-SA 3.0** (© Ulrich Apel / contributors); the generated `assets/stroke/kana_strokes.json` is a derivative and must stay CC BY-SA with attribution (embedded in the file's `source`/`license` fields). The app bundling it is an aggregation and is unaffected. **Human action:** confirm the CC BY-SA attribution is acceptable for the commercial build (it is standard practice; most kanji apps ship KanjiVG-derived data this way).

**D-013 | 2026-07-10 | WritingScreen canvas adapts to the shorter axis; no app-wide portrait lock.** The square drawing paper was `AspectRatio(1)` sized by width inside a non-scrolling Column → hundreds of px of overflow on any short viewport (landscape, split-screen, the default 800x600 flutter_test surface). Considered `SystemChrome.setPreferredOrientations` (portrait lock, matches the budget-phone target) but rejected it as the primary fix: Android multi-window/split-screen ignores orientation preferences, so a locked app can still receive a short viewport and would still overflow. Instead the paper is `Expanded > Center > AspectRatio(1)` — square sized by min(width, remaining height), portrait rendering unchanged (still effectively width-sized on tall screens). Regression-tested at 800x600 in `test/widget_test.dart`. A portrait lock remains an optional later product choice (D-012 slot is reserved by the pending whitelist proposal in NEXT_SESSION.md). Supersedes nothing.

**D-014 | 2026-07-10 | Android toolchain pinned to the installed Flutter SDK's template versions; Kotlin incremental compilation disabled.** The hand-mixed scaffold pinned AGP 8.3.0 / Kotlin 1.9.22 / Gradle 8.7 — below Flutter 3.44.5's AGP floor (8.6.0), so no device build had ever succeeded. Now pinned to exactly what `flutter_tools/gradle_utils.dart` templates: **AGP 9.0.1, Kotlin 2.3.20, Gradle 9.1.0** (rule: when bumping the toolchain, read the installed SDK's template constants rather than guessing versions). `kotlin.incremental=false` in `android/gradle.properties` because Kotlin's incremental-cache path converter cannot relativize across Windows drive roots (pub cache on C:, project on D:) and fails every plugin compile ("this and base files have different roots"); non-incremental costs seconds at this project size. First successful device build+install+launch verified on TECNO LG7n (Impeller/Vulkan, no fatal exceptions).

---
_New decisions: append below in the same format. Every LLM/dev that makes a spec-silent choice MUST add an entry._

**D-014 | 2026-07-10 | Content factory merged from session zips; clean-zip version is authoritative; kana rule made falsifiable.** The repo's `content_factory/` held truncated hand-merged stubs (empty `__init__.py`s, stripped `schemas`/`build.py`, 200-byte source stubs) while the full working pipeline lived only in `sensei_content_factory_v4.2.zip` / `sensei_extracted_clean.zip` at the workspace root. Merged the *clean* zip (the debugged build that produced 81 cards — see `content_factory/BUILD_SUMMARY.md`) plus the v4.2 test suite. Two reconciliations: (1) grammar test fixture updated to 2 examples — the ≥2-examples rule is intentional in both zip versions and the real N5 data has 3 each; (2) `VocabularyValidator._is_valid_kana` no longer whitelists LATIN — it made rule #3 ("reading must be kana") unfalsifiable for English text; the v4.2 test encoding the correct contract now passes. Verified end-to-end: 4/4 pytest, pipeline 20/20+3/3+5/5 valid → 81 cards → `pak_sensei_n5_core` (0.15 MB). Windows note: run with `PYTHONUTF8=1` (the pipeline prints emoji; cp1252 consoles crash).

**D-015 | 2026-07-11 | PROPOSED — Learn re-architected as goal-based journey maps + AI classrooms.** Owner direction: (a) onboarding gains a goal question (SSW work / JLPT study / daily life); each goal renders its own Japan journey map over the SAME verified pack DAG — goal changes sequencing/emphasis (Director config), never content; (b) each lesson node opens an "AI classroom" scene where the Persona avatar stages the existing 5-phase micro-loop and the agent bus drives the staging (psych states from 09 become scene lighting/motion/teacher behavior); (c) constraints reaffirmed: no locked nodes (recommended path only, D-001), fixed mastery rewards (passport stamps = existing milestone schedule), animation obeys budget-phone + reduced-motion gates, agents stay deterministic (D-004) — the classroom is presentation over the existing agent/FSRS engine, not a new intelligence layer. Design-first: screens come from the Claude design project per DESIGN_BRIEF.md §2; code follows handoffs. Confirm scope at implementation: map = Learn tab replacement; classroom = LessonScreen replacement.

**D-011 | 2026-07 | Curriculum ontology adopted as single source of truth.** `assets/curriculum/curriculum.json` (levels + Can-do units + prerequisites + per-level whitelist) drives RAG scope, LLM whitelist/GBNF, deterministic grader keys, FSRS deck + unlock order, Director sequencing, and pack boundaries. Reason: bounds the on-device LLM (safety/correctness) and aligns the whole app to JFT-Basic A2 / JLPT N4. Supersedes ad-hoc lesson ordering. See CURRICULUM_MAP.md, DESIGN_HANDOFF.md §0.

**D-012 | 2026-07 | AI Classroom adopted as the flagship adaptive lesson surface.** [AI CLASSROOM (added 2026-07-11)] `lib/presentation/lesson_screen_v4.dart`. One classroom scene, five ambient MOODS (neutral/flow/struggle/burnout/boredom) that map 1:1 onto the 09_UI_STATES psychological states — mood is chosen DETERMINISTICALLY from answer correctness/streak (never LLM, D-004). The sensei chat/talk sheet is EXPLANATORY only; grading stays answer-key (D-001/correctness). Mood shifts are ambient (color/copy), never blocking or shaming; Skip/Hint/Quit stay always-available. Section ink = `aiClassroomRed #B3121B` (EXCLUSIVE to this surface).

**D-013 | 2026-07 | AI Classroom is the teaching hub (curriculum + book + Sensei chat).** [AI CLASSROOM HUB (added 2026-07-11)] The classroom hosts three surfaces:
(1) **Curriculum section** — the Can-do ladder browser reading `assets/curriculum/curriculum.json` (L0→A1→A2→N4); pick a unit → Sensei teaches it.
(2) **Book section** — an offline reference reader aligned to Irodori/JF-Standard Can-dos (verified content only; no copyrighted text — our own authored material).
(3) **Sensei chat/talk tutor** — voice + chat optimized: STT in, TTS/native audio out, Banglish explanations.
**Sensei follows the teaching logic (binding):** teaches ONLY curriculum-scoped, whitelist-bounded, verified content; comprehensible-input sequencing (i+1) driven by the Director agent + prerequisite graph; explanations retrieved/paraphrased, never invented (D-004/correctness); chat is EXPLANATORY only — **grading stays answer-key** (D-001). Mood/affect is deterministic (04 agents), never LLM-judged. Everything degrades to offline; cloud tutor is an opt-in enhancement.

**D-016 | 2026-07-16 | Owner's Master Architect Prompt adopted as the binding product-direction charter (`docs/13_MASTER_VISION.md`).** The AI Classroom IS the product: conversation-first, voice-first, Bengali-first teaching with a proficiency-based BN↔JP language balance (80/20 → 50/50 → 20/80); proactive teacher behavior (never a chatbot); a 13-stage classroom flow that EXTENDS the existing 09 5-phase micro-loop; character learning that ends in real communication; friendly explain-why error correction. Reconciliations (constitution wins, per the prompt's own core rule): (1) streaks = neutral history only, no pressure mechanics (D-001); (2) mastery validation is recommended assessment, never a lock (D-001/D-015); (3) "curriculum delivered through conversation" means the conversation is the delivery SURFACE — content/grading stay verified + deterministic, the LLM selects/glues/phrases only (D-004/D-011/D-012). Current built state is preserved and extended, not replaced.

**D-017 | 2026-07-16 | BN↔JP language-balance mapping (13_MASTER_VISION execution).** The sensei's dynamic Bengali↔Japanese mix is driven by the learner's CURRENT curriculum level (`learnerLevelProvider`, derived from the ladder's current unit): L0/A1 → beginner (80–90% Bengali, Japanese always with romaji + Bengali pronunciation) · A2 → intermediate (≈50/50, Japanese sentence first then Bengali explanation) · N4 → advanced (80–90% Japanese, Bengali only for hard spots). Injected as a system-prompt suffix in AiTutorService (reply/explain); prompt-level steering only — content/grading stay verified + deterministic. Spec was silent on the level→ratio mapping; smallest reasonable choice, revisit when real proficiency signals (agent bus mastery) exist.

**D-018 | 2026-07-16 | Backend chosen: SUPABASE (closes D-010).** Owner provisioned a Supabase project (ap-northeast-1/Tokyo — closest region to the learners' target country). Applied `supabase/schema.sql` v1: profiles, srs_cards (FSRS mirror), lesson_completions, daily_stats, deletion_requests — per docs/07 sync contract (delta, device-wins, 30d retention, deletion w/ grace). SECURITY: RLS enabled on every table with own-rows-only policies (auth.uid() = user_id); verified anon role sees nothing; content itself never syncs (bundled+verified on device); DB connection string lives OUTSIDE the repo (../.claude/sensei_db.env, chmod 600) — the client will use only the project URL + anon key via supabase_flutter (next slice). Owner must ROTATE the DB password (was posted in chat). Cloud remains OPTIONAL — offline-first unchanged (D-001/00).

**D-019 | 2026-07-16 | PROJECT SCALE adopted as the master completion tracker.** `PROJECT_SCALE.md` (repo root) = the ordered, dependency-gated step list to finish the project (Phases A Content → B Practice → C Vision → D Platform → E Launch + owner-only items); `tools/progress_scale.mjs` = the machine validator (13 automated proofs + content-% scoreboard + names the next unblocked step; `--gate` adds flutter analyze+test). Working rule: one step at a time, gate green before AND after, tick the box, update CODEBASE_MAP/NEXT_SESSION, commit. Wired into CLAUDE.md read order so every session loads it — this is the project's persistent memory of remaining work. Percentages are computed, never hand-edited.

**D-020 | 2026-07-17 | N4 whitelist authored; validator level-scoped (step A2).** `content_factory/n4_whitelist.txt` = 1402 words: the complete JFT-A2 list (superset per curriculum.json "n4: JLPT N4 list superset") + ~300 curated entries from the standard public JLPT-N4 vocabulary canon (kana surface forms, as lessons use). `tools/validate_content.mjs` now selects the bound by the lesson's `jlpt_or_jft` tag — N4-tagged lessons check n4, everything else stays inside jft_a2 (D-011). The list bounds what N4 lessons may teach; it is NOT itself teaching content. Native-speaker review of the N4 canon remains pending with all other content (A5).

**D-021 | 2026-07-17 | Offline LLM fallback LIVE on desktop (llama.cpp + Qwen3-1.7B Q4_K_M, arch 08).** Downloaded per owner instruction: llama.cpp b10052 prebuilt + Qwen3-1.7B-Q4_K_M.gguf (~1.1GB, unsloth mirror of the spec model) — both OUTSIDE the repo (../.claude/llm/). `tools/run_local_llm.sh` starts an OpenAI-compatible llama-server on 127.0.0.1:8089; the AI proxy's failover chain is now DeepSeek→Kimi→OpenAI→**local-qwen3** (think-tags stripped server-side), PROVEN end-to-end with zero cloud keys (`ai: local-qwen3 → 200`). Measured ~7.6 tok/s on this CPU VM (spec gate: >8 on device — close; real benchmark stays D2/D4). HONEST QUALITY NOTE: raw 1.7B Bengali is weak (mistranslated みず) — matches the spec's own ladder (raw Qwen 6.5/10; needs RAG/whitelist grounding). So: cloud = quality path, local = availability fallback, verified store (explainOffline) = the truth layer. Android on-device integration remains step D4.

**D-022 | 2026-07-17 | Cloud sync client built (D1) — anonymous auth, opt-in, idempotent snapshot.** supabase_flutter added; Supabase.initialize runs at startup (public anon key embedded — safe, RLS guards data per D-018). SyncService: anonymous sign-in (zero friction, offline-first spec), then an idempotent upsert of a progress snapshot (profiles.last_sync + daily_stats from existing local counts: retained words / completions / due). Settings gains an opt-in toggle (OFF by default; copy stresses the app works fully offline and data stays device-safe). All failures degrade silently. NOTE: the Supabase project has Anonymous sign-ins DISABLED, so sync stays off until the owner enables it (Auth → Providers) — the toggle surfaces a clear message meanwhile. Full per-card srs_cards delta sync (device-wins, higher-stability merge) + offline_queue are the next increment; this slice proves the whole auth→RLS→write chain.

**D-023 | 2026-07-17 | l10n migration completed — gen-l10n re-enabled (closes the "l10n TRAP").** The `lib/l10n/app_{bn,en,ja}.arb` files now carry the ~20 strings the code actually localizes (template = app_en.arb), `l10n.yaml` restored, `pubspec flutter: generate: true`. Verified: `flutter gen-l10n` regenerates `app_localizations.dart` with all getters intact (no wipe), `flutter analyze` clean, and a full `flutter build web` (which runs gen-l10n) succeeds — so `flutter build apk` will not fail on l10n. Rule going forward: a NEW localized string must be added to ALL three ARBs. Supersedes the standing "Do NOT run gen-l10n" warning in CLAUDE.md/CODEBASE_MAP (now marked resolved).

**D-024 | 2026-07-17 | Real voice input for talk-to-sensei (replaces the fake mic).** The sensei-chat mic was a demo (waveform → hardcoded canned message). Now `speech_to_text ^7` (device/browser native recognizer) powers `VoiceInputService`: tap mic → real STT (Japanese first for speaking practice, Bengali fallback) → live transcript fills the input → stop → sends to the AI → sensei replies (LLM) → reads back (TTS). This is CONVERSATION practice, ungraded — distinct from D-002 (which governs *scored* pronunciation via whisper forced-alignment, still D5). Graceful: no recognizer / permission denied / unsupported → snackbar + text box stays (never blocks, D-001). Android manifest gains the RecognitionService <queries> intent (Android 11+ visibility); RECORD_AUDIO already present. Web uses the browser Web Speech API. On-device Japanese/Bengali recognition depends on the OS having those language packs (most Androids do, often needs network). This closes the biggest voice-first gap: the learner can now SPEAK to the sensei, not just type.

**D-025 | 2026-07-17 | Offline sensei made SAFE — raw local LLM removed from the learner-facing fallback (correctness).** Reversing part of D-021: a raw Qwen3-1.7B has weak Bengali↔Japanese and can INVENT grammar, which violates the #1 rule "correctness over generation" (docs/00). So the local llama-server is no longer in the proxy's auto-failover chain — it is gated behind `ENABLE_LOCAL_LLM=1` (dev-only, output explicitly `unverified`). When every cloud provider fails, the proxy returns nothing → the app falls back to VERIFIED content only: `ContentRepository.explainOffline` (copy-explain) and `handleOfflineChat` (chat), both of which return real lesson/kana/pitch data and REFUSE to fabricate (e.g. "অফলাইনে নতুন উদাহরণ তৈরি করা যাচ্ছে না")। The chat's old generic `_canned` list (which showed "水=পানি" for any question) is replaced by an honest offline line: "এই প্রশ্নের উত্তর বানিয়ে বললে ভুল হতে পারে, তাই বলছি না … যেকোনো শব্দ select করো, verified ব্যাখ্যা দেব." Net effect: offline, the sensei is never wrong — it either gives verified content or honestly declines. Real offline AI = the CONSTRAINED on-device path (GBNF + whitelist + RAG), step D4 — not a raw server.

**D-026 | 2026-07-18 | UX debug pass — every user-visible number is now REAL or an honest empty state (no fabricated data anywhere).** Swept every reachable screen and removed all fake/placeholder data shown to learners, closing the last "correctness over generation" gaps in the UI: (1) **AI Check exam** — the Home card + Progress `psychology` icon opened `AiCheckScreen`, which passed/failed on `DateTime.now().millisecond.isEven` (a coin-flip) with hardcoded Banglish stats; both now open the real deterministic `MockExamScreen(kind:'jft')` (answer-key graded, D-001), and the dead `AiCheckScreen`/`_AlertRow` are deleted. (2) **Progress screen** — hardcoded `N5 · 72%` level chip → real per-level completion chips derived from `curriculumProvider` (current level ink-filled, chosen goal from `goalProvider` as a pink chip); demo `শোনা 55%`/`বলা 32%` skill blocks → real `মনে রাখা` (retention avg from `SrsLocal.retentionByDay`) + `সম্পন্ন` (mean unit progress); the fabricated demo retention series (58→72 for zero-history users) → honest empty state. (3) **Home topic row** — 4 hardcoded topics (かな 64% / 買い物 42% / 挨拶 80% / 仕事 18%) → real units from `curriculumProvider` (level chip + title + live pct). (4) **ShadowingScreen** — the pitch "score /100" was computed from a hardcoded `learner` contour (same number regardless of what you said); replaced with a REAL device-STT self-check via `VoiceInputService` (transcribe → lenient match against よろしくおねがいします → honest "শোনা গেছে: …" + ✓/retry, never a fabricated score; degrades honestly when no recognizer). (5) **KanaScreen** — dead 🔊 button (`/* TODO: ttsService */`) → real `AudioService.play('kana_hira_<romaji>')`. (6) **Dead code removed** — the unreachable v0.1 `LessonScreen` (3 dead 🔊/record buttons) + its only caller `lesson_list_screen.dart`. Verified: `flutter analyze` clean, all 68 tests pass (incl. the "onboarding → every screen, no crashes" journey, updated to the real MockExam flow), `flutter build web` compiles. Real pitch-accent f0 scoring remains the on-device D4/D5 step; roleplay stays authored branching dialogue (deterministic, honest).

**D-027 | 2026-07-18 | N4 mock exam aligned to the official JLPT blueprint (real section times).** Per the jlpt.jp "Test sections and test times" reference, the N4 mock now carries the official 3-section structure AND times: Vocabulary (文字・語彙) 25min · Grammar·Reading (文法・読解) 55min · Listening (聴解) 35min = **115min total** (was a flat 50min). `MockSection` gained an optional `minutes` field; the exam-intro card shows per-section time. Question counts scaled toward the real exam and weighted like it (14 vocab / 16 grammar·reading / 12 listening = 42, up from 39) — still a content-bounded practice SUBSET drawn only from verified items (D-004), and the result screen keeps the honest "practice estimate, not an official score" banner. JFT-Basic mock left unchanged (different test, not covered by the JLPT sheet). Per-item-type 問題 granularity (Kanji reading / Orthography / Sentential grammar 1&2 / etc.) deferred — needs the content tagged by item-type first; not faked. Tests updated (N4 = 42Q, minutes 115, sections [25,55,35]); all 68 green.

**D-028 | 2026-07-18 | Scope expanded to full JLPT ladder (N5→N1) — code scaffolding shipped; content is a native-reviewed program.** Owner reframed the product from JFT-A2/N4 to **basic→advanced up to N1** ("speak like native") covering all jlpt.jp item types. Plan (`.claude/plans/bright-greeting-graham.md`): the app's architecture already scales (data-driven `curriculum.json`, level-keyed providers), so the CODE is modest; the mountain is **verified content** (constitution D-004 bans fabrication → every N3/N2/N1 item native-reviewed before `verified:true`). First advanced milestone = **N2**. Recommended sourcing = hybrid (open datasets backbone + LLM localization drafts + hard native-review gate). **This session shipped Phase 0–1 scaffolding (code only, no fake content):** (1) `curriculum.json` gains N3/N2/N1 levels + whitelist notes + one mock unit each (N3.M/N2.M/N1.M, chained), showing the roadmap; the N-level mocks honestly return "content coming" until authored. (2) `item_type` field on `LessonItem` (Dart) + `ExamItemType` enum (Pydantic) covering the 20 official 問題 types — nullable/back-compat. (3) `validate_content.mjs` picks the whitelist by level (n4/n3/n2/n1), scaffold-skipping absent lists (no false-fail). (4) `mock_exam.dart` generalized from `jft|n4` to a level-parameterized builder matching the official blueprint — **N5/N4/N3 = 3 sections** (Vocab / Grammar·Reading / Listening), **N2/N1 = 2 sections** (combined Language Knowledge·Reading / Listening) — with official section times and published JLPT pass marks (N5 80 / N4 90 / N3 95 / N2 90 / N1 100, out of 180); `MockExamScreen.kindForUnit` maps mock units to kinds. (5) BN↔JP balance (D-017) extended: N3 advanced+ · N2 near-native · N1 native-like. Still DEFERRED (needs humans/new capability): N3/N2/N1 content authoring + native review; per-問題-type sub-blocks (needs content tagged by `item_type`); "speak like native" spoken-production assessment (pronunciation/pitch scoring D5) + on-device generative AI (D4). Verified: `flutter analyze` clean · 71 tests green (N5/N2 structure, honest-null for unbuilt, kindForUnit) · scale gate green. Recommendation: ship advanced levels as additive packs; do NOT block the current N4/JFT SSW product on N1.

**D-029 | 2026-07-18 | Banglish style contract pinned into every LLM prompt (fixes "AI-এর বাংলা translated লাগে").** Owner observed the AI's Bengali reads like it was translated from English — correct diagnosis: cloud models (DeepSeek/Kimi/GPT) default to stiff textbook Bengali (আপনি/এটি/"নিশ্চিত করুন" register) and one vague "Smart Banglish" line can't override that. Fix: `_styleContract` in `ai_tutor_service.dart`, appended to BOTH system prompts (chat + dictionary): hard register rules (তুমি never আপনি; এটা never এটি; করো never করুন), an explicit ban-list of translationese calques, natural code-mixing guidance (word/practice/tension stay English — no forced পরিভাষা), spoken-rhythm markers (তো/না?/আচ্ছা), and a few-shot ✗bad/✓good example pair (the strongest lever for register). Scanned all AUTHORED text (book.json blocks, 714 lesson notes, UI strings) for translationese markers → 0 hits in content; only 2 stiff strings in writing_screen.dart fixed (এটি→এটা, formal hint → conversational). Style-only change — correctness rules (never invent grammar, D-004) untouched. Verify: needs a live provider key to A/B the reply register; owner (native speaker) is the reviewer for Bengali register.

**D-030 | 2026-07-18 | Owner's Teaching Philosophy adopted as the binding classroom pedagogy (`docs/14_TEACHING_PHILOSOPHY.md`).** Global rule for the AI tutor across EVERY module: curriculum-driven order, never assume untaught knowledge, interactive step-by-step conversation (not lectures), confirm understanding before moving on, connect new material to earlier lessons, reteach differently on struggle, scaffold-fade as level rises, consistent across vocabulary/grammar/reading/writing/listening/speaking/culture. Reconciliations (constitution wins, same rule as D-016): "unlock after mastery" → RECOMMEND never lock (D-001); "monitor confidence/speed" → deterministic agent-bus signals only (04/D-004); "correct mistakes" → answer-key grades, LLM only explains why. EXECUTED now: (1) `_teachingContract` appended to the sensei chat system prompt (ai_tutor_service.dart) — step-by-step + check-understanding + connect-to-old + reteach-differently + scaffold-fade + never-force, in the D-029 Banglish register; (2) **taught-scope awareness** — `SenseiChatSheet._hint` now builds "শেখা শেষ: <done units> · এখন শিখছে: <current unit>" from the live curriculumProvider and sends it with every chat/explain call from every entry point (classroom previously sent NO curriculum context), so the tutor can actually honor "never assume prior knowledge"; scope instruction says teach-before-use for anything outside it. Routed in 00_START_HERE. Future per the charter: tutor-generated exercises (blocked on D4 constrained on-device path + D-028 item-type content), scored speaking (D5).

**D-031 | 2026-07-18 | Tiered LLM routing (cost ↓, teaching quality ↑) + mastery feeds Progress.** Owner directives: use the strongest models (Claude/Gemini/GPT) where TEACHING quality matters, free/cheap models for small answers to cut API cost; and "after learning any concept perfectly it should be added on progress". (1) **Proxy** (`tools/web_server.mjs`): requests now carry `tier` — `'quick'` → CHEAP chain (DeepSeek → Kimi → Gemini 2.5 Flash → GPT-4o-mini), `'teach'` → TEACH chain (**Claude `claude-opus-4-8` via the native Anthropic Messages API** — raw HTTPS, x-api-key, OpenAI-shape adapted both ways so the Flutter client is unchanged → Gemini 2.5 Pro → GPT-4o), falling back to the cheap chain, then the app's verified-offline fallback (D-025 unchanged — never fabricated content). Claude `stop_reason: refusal` and any ≥400 fail over to the next provider. Keys via env only: ANTHROPIC_API_KEY / GEMINI_API_KEY / DEEPSEEK_API_KEY / MOONSHOT_API_KEY / OPENAI_API_KEY — a provider without a key simply isn't in the chain. (2) **Client** (`ai_tutor_service.dart`): `reply()` (the teaching conversation) = `tier:'teach'`; `explain()` (dictionary lookups, high-frequency) = `tier:'quick'`. (3) **Mastery → Progress**: `masteredCountProvider` surfaces `SrsLocal.retainedWordCount()` (FSRS stability ≥ 7 days = learned well enough to stick a week+) on the Progress screen as "🏆 আয়ত্তে: N শব্দ" — a real memory-model signal, never LLM-judged, hidden until N>0. On "it shouldn't be offline-first if it doesn't work accordingly": interpreted as BOTH modes must genuinely work — offline-first (constitution #2) stands (core works offline, honest fallbacks), while online now uses the best model available instead of being artificially limited. Verified: proxy syntax + live tier smoke (both tiers fail over to 502 with no keys; app 200), analyze clean, 71 tests green, gate green.

**D-032 | 2026-07-18 | Brand & sensory overhaul — real identity, no default-Flutter anywhere, learner-paced audio.** Owner audit feedback: "icons are not professional" + "sound system sounds worst, nothing attractive". Diagnosis: the app shipped the DEFAULT FLUTTER LOGO as launcher/web/favicon, was named "sensei_app" on the phone/browser tab/PWA manifest, flashed a blank white page for 10-20s while the web engine booted (and a white Android launch screen), and the (already-neural) edge-tts clips were generated at full native speed — abrupt/rushed for learners. Fixes: (1) `tools/make_icons.py` — branded icon (yellow ば in the app's own Zen Kaku Gothic Black on brand #111 + green dot) generated for all Android mipmap densities, web 192/512 + maskable + favicon; (2) app label/title/manifest → **Bhasago** everywhere; (3) dark branded boot splash on web (pulsing icon + "লোড হচ্ছে…", removed on flutter-first-frame) and Android (launch_background = brand bg + icon); (4) ALL 975 audio clips regenerated with `rate="-12%", pitch="+5Hz"` (learner-paced, warmer single-word delivery) — generator gains `--force`; voice stays ja-JP-NanamiNeural (neural, was never the problem — pacing was). KNOWN LIMIT, honest: Bengali TTS for dynamic sensei replies uses the device/browser voice — robotic on web; a server-side edge-tts /ai/tts endpoint (bn-BD-NabanitaNeural) is the follow-up if the owner wants attractive Bengali speech online. Pre-store blocker flagged: applicationId still com.example.sensei_app (Play Store rejects com.example) — must change before first upload.

**D-033 | 2026-07-18 | "Complete remaining" push — store identity, E1 privacy, neural sensei speech, D2 rebuilt.** (1) **applicationId → `com.bhasago.app`** (was com.example.sensei_app — Play Store rejects com.example and the ID is immutable after first upload; changed NOW, before any distribution. Side effect: the next APK installs as a NEW app beside the old one — owner uninstalls the old). (2) **E1 done:** `privacy_screen.dart` — Bengali-first, plain-language, and strictly honest to actual practice (on-device encrypted data, opt-in anonymous sync, question-text-only to AI when online, device-recognizer voice, 1-tap export/delete w/ grace, no ads/tracking/analytics); wired into Settings; must be updated whenever practices change. (3) **Neural sensei speech (fixes robotic Bengali):** proxy gains `GET /ai/tts?text&voice=bn|ja` — spawns the repo's edge-tts (execFile argv = injection-safe, 600-char cap, sha1-cached in .tts-cache/, rate -8%) with bn-BD-NabanitaNeural / ja-JP-NanamiNeural; client `AiTutorService.ttsUrl()` + chat sheet plays neural first (just_audio, 8s timeout) → device-TTS fallback → honest no-voice message. Verified live: first synth 3.3s, cached 1ms, valid MPEG. (4) **D2 re-run on this machine** (toolchain persists): APK rebuilt with icon/name/splash/audio/AI/privacy — all six decision-loads. Tracker: D2 ✓ (benchmarks still open), E1 ✓ (store data-safety form due at E4). NOT done (still honestly blocked): A5 native review (human), D4/D5 (device-native toolchain), E2 payments (owner accounts), E3 UAT (humans), N3+ content (needs dataset-import + native review); D3 packs deliberately skipped for beta (bundle size fine).

**D-034 | 2026-07-19 | YouTube-method research → kana picture-story mnemonics (Bengali-localized).** Owner: "analyse YouTube videos to make it better." Honest scope: can't watch video; researched what the most-watched kana/Japanese-learning methods (Tofugu mnemonic charts, Dr. Moku, JapanesePod101 "kana in 1 hour", comprehensible-input channels) do and what app learners complain about (Duolingo critiques: romaji crutch, no direction, gamification-over-learning, untaught vocab, no speaking). Gap analysis: the app already had comprehensible-input sequencing (DAG+taught-scope), direction (journey map), no dark gamification (constitution), taught-scope guard, speaking practice — the ONE missing top-technique was **picture-story mnemonics** ("brains love stories, hate abstract lists"). Shipped: `mnemonic_bn` on all 92 kana entries (hiragana+katakana JSON; Bengali SHAPE associations — e.g. し=বড়শি, ぬ=কাঁটাচামচে নুডলস, ツ vs シ disambiguation) → `KanaEntry.mnemonicBn` → surfaced at BOTH teaching moments: WritingScreen sensei line (💡 hook with each char) and KanaScreen grid tap (audio + snackbar). Mnemonics are memory AIDS (shape descriptions), not language facts — still queued for A5 native review like all copy. Deferred from the research: romaji-weaning toggle (hide romaji as level rises — good candidate next); per-video content analysis (needs actual video access). Verified: analyze clean · 71 tests · content validator PASS.

**D-035 | 2026-07-19 | শব্দভাণ্ডার — browsable vocabulary bank.** Owner asked "is there a vocabulary learning section" — vocab was TAUGHT (classroom micro-loop) and REVIEWED (SRS due-cards) but not BROWSABLE. New `vocab_screen.dart`: every course word grouped by live curriculum-ladder order, full-text search (bn/en/jp/kana/romaji), per-word REAL status from SRS (🟢 আয়ত্তে = stability ≥7d per D-031 · 🟡 শিখছি = seeded · ⚪ সামনে = not met), header counts, 🔊 bundled audio per word, and tap → the unified SenseiChatSheet seeded with that word (chatKey vocab:<id>). Browsing is free — status is information, never a lock (D-001). Entry: Home AppBar (style icon) before the dictionary. Verified: analyze clean, 71 tests green.

**D-035 | 2026-07-19 | শব্দভাণ্ডার — browsable vocabulary bank added.** Owner asked "is there a vocabulary learning section" — vocab was TAUGHT (classroom micro-loop) and REVIEWED (SRS due-cards) but there was no place to BROWSE the whole word bank. New `vocab_screen.dart`: every course word grouped by curriculum-ladder order (same sequence the classroom teaches), live search (Bengali meaning / jp / kana / romaji), per-word 🔊 (bundled clip), and REAL per-word status from the SRS store — 🟢 আয়ত্তে (FSRS stability ≥7d, D-031 definition) · 🟡 শিখছি (seeded) · ⚪ সামনে (not met) — never a guessed status. Tapping a word opens the SAME unified sensei chat seeded with it (chatKey vocab:<id>). Header counts mastered/learning/total. Entry: Home AppBar (style icon) next to the dictionary. Browsing is free — no locks (D-001).

**D-036 | 2026-07-19 | Classroom-integration audit + flexible vocab practice + 48-item content batch.** Owner: "integrate everything with the classroom, build more vocabulary, check everything is wired per plan." (1) **Wiring audit:** classroom ✓ curriculum-batch, ✓ SRS seeding, ✓ agent-bus moods, ✓ 5-phase loop, ✓ next-recommendation, ✓ taught-scope chat (D-030). TWO real gaps found & fixed: (a) **FSRS progress-reset bug** — `seedCard` used ConflictAlgorithm.replace, so replaying a lesson wiped its words' stability back to newCard; now ConflictAlgorithm.ignore (existing progress always wins); (b) **classroom could only teach the current unit** — no re-practice path. (2) **Vocabulary training flexibility:** `buildClassroomBatch(forceLessonId:)` + `practiceBatchProvider(family)` + `LessonScreenV4(practiceLessonId:)` + "ক্লাসরুমে অনুশীলন করো" button on every vocab-bank lesson block — ANY lesson practicable anytime in the full classroom experience (D-001: recommend-never-force now includes re-practice). Completion re-records honestly; SRS progress preserved by (a). (3) **Content batch:** 6 new SSW-daily-life lessons × 8 items = 48 (colors, money/bank, ゴミ分別 garbage rules, apartment, post office, everyday phone) — standard N5 canon, trilingual, wired into A1.3/A2.3/A2.4/A2.5; 25 standard words appended to jft_a2_whitelist (documented block); 48 neural clips generated; book synced (762 rows). Ladder now **854/772 = 111%**. All 13 proofs + analyze + 71 tests green.

**D-037 | 2026-07-19 | Romaji-weaning toggle + N3/N2/N1 whitelist backbone imported ("build all" close-out).** (1) **Romaji weaning** (top remaining item from the D-034 YouTube research — the #1 critique of language apps is keeping learners on the romaji crutch): Settings → "শেখার ধরন" gains a "রোমাজি দেখাও" switch (default ON; learner-controlled, never automatic — D-001). When OFF, romaji disappears from the classroom reading lines (intro/recognition/say-it cards show kana only) and the vocab bank titles. Writing screen keeps romaji (that's the kana-LEARNING surface where it belongs). (2) **Advanced-ladder content backbone** (executes D-028 hybrid-pipeline step 1 — dataset-backed, zero invented Japanese): `tools/import_jlpt_list.py` imports the open JLPT deck (jamsinclair/open-anki-jlpt-decks, from Jonathan Waller's CC-BY lists) → **n3_whitelist 3,665 forms · n2 3,414 · n1 4,931** (expression + reading surfaces). Validator now bounds all levels (n4:1636 n3:3665 n2:3414 n1:4931). These BOUND future N3+ lessons; actual lesson authoring + native review (verified:true gate) remains the content program. Verified: analyze clean · 71 tests · validator PASS · gate green · web+APK rebuilt.

**D-038 | 2026-07-19 | Full project re-audit — all passes green; 3 hygiene fixes.** Six-pass audit: (1) automated proofs 13/13 + analyze clean + 71 tests + validator PASS; (2) fake-data/dead-control regression scan — CLEAN (D-026 standard holds across all new screens); (3) content integrity — 1023/1023 audio clips present, 92/92 kana mnemonics, 96 lesson files all trilingual + verified:true; (4) security — no API tokens/passwords in the repo (only the Supabase ANON key, public by design under RLS); (5) navigation — one true orphan found: v0.1 `agent_panel.dart` (137 lines, zero refs) → deleted; `progress_screen.dart` stays (documented T-108 query reference); (6) hygiene — `.tts-cache/` (2 test mp3s) untracked + gitignored; **version bumped 0.1.0+1 → 0.9.0+2** (pubspec + Settings label, was stale "Internal SENSEI"). Also verified build/ untracked (an earlier scan warning was a shell-logic false positive — double-checked with `git ls-files | wc -l`). Standing risk items unchanged and still owner-gated: exposed GitHub token in chat history, Supabase DB password rotation, old OpenAI key, AI provider keys absent (teach tier dark), applicationId ✓ fixed, native review pending.

**D-039 | 2026-07-19 | Design-system pass — icon 2.0 + rounded iconography + unified control theming.** Owner: "upgrade all icon design, design everything UI/UX." (1) **Icon 2.0** (`make_icons.py` v2): subtle vertical gradient tile (#161616→#0D0D0D), faint brand red-sun disc upper-right (ties the launcher to the Home backdrop motif), ば with soft drop shadow, green live-dot with contrast ring (reads at 48px), rounded corners (22%) on web-displayed variants — Android legacy mipmaps stay square (launcher masks). (2) **Rounded icon family** across nav: Home AppBar (style/translate/grid_view/draw/settings → _rounded) and NavigationBar gains selectedIcon states (home/school/mic/monitor_heart filled-rounded when active) — warmer, consistent with the stadium-pill v4 language. Tests updated (tapHome helper handles the selected-state glyph swap). (3) **Theme polish** (theme.dart): unified ListTile/ExpansionTile shape+icon color, ProgressIndicator brand-yellow with quiet tracks, Tooltip surface, default IconTheme ink, TextField focus ring → brand yellow (was ink white). No layout rewrites — the v4 Bold Ink system stays; this pass tightens consistency. Verified: analyze clean · 71 tests · icons regenerated · web+APK rebuilt.

**D-040 | 2026-07-20 | Play-publish kit — release signing, signed AAB, store assets (owner does the Console steps).** Owner: "publish now on Google." Built everything mechanizable: (1) **Release upload keystore** generated (`~/.bhasago-upload-keystore.jks`, RSA-2048, valid→2053) + `android/key.properties` (gitignored); `build.gradle.kts` now signs release with it (falls back to debug when key.properties absent, so CI/others still build) + minify/shrink + proguard keep-rules (flutter/tink). (2) **Signed App Bundle** built & verified (`~/Downloads/bhasago-release.aab`, 77MB, cert Owner=CN=Bhasago — real upload key, not debug) — this is Play's required upload format. (3) **Store assets** in `store_assets/`: `play_icon_512.png`, `feature_graphic_1024x500.png`, `PLAY_LISTING.md` (name/short/full desc bn+en, honest Data-safety answers matching privacy_screen, content-rating guidance, signing/App-Signing notes). (4) **Privacy policy** `docs/privacy.html` (host-ready, bn+en, matches the in-app policy) — Play requires a public URL; enable GitHub Pages or any static host. OWNER-ONLY (can't be automated): Play Developer account ($25), Console listing entry, **2–8 phone screenshots** (CanvasKit not reliably screenshottable here — take from the installed app), form submission, privacy URL hosting, back up the keystore (loss = can never update). RECOMMENDED: release to Internal/Closed testing first (instant, no review wait, real users) — the responsible way to "publish now" while A5 native review is still pending, rather than exposing unreviewed content to the open public. applicationId com.bhasago.app ✓; version 0.9.0+2.

**D-041 | 2026-07-21 | Language switch actually switches teaching content + AI (not just 20 UI strings).** Owner: "when I select English the whole thing — content, AI, curriculum, book, everything — should change, instantly." Diagnosis: the picker set `localeProvider` and MaterialApp rebuilt, but only ~20 ARB strings were wired; ~797 hardcoded Bengali literals + 76 `.bn` content reads ignored it (Bengali-first by design, D-001). Owner chose scope **"wire up existing data now"** (the trilingual `Tri`/curriculum data already exists — a wiring job, not translation). Delivered: (1) **Persistence** — `main.dart` restores `locale_chosen` at startup via ProviderScope override (was always defaulting to bn); new `setAppLocale()` helper + `langProvider` (reactive languageCode); Settings now persists. (2) **Content reads locale** — `CurriculumUnit` now carries trilingual `Tri title/canDo` (tolerant parse, bn fallback); journey map, curriculum screen, home, vocab bank render `.of(lang)`. (3) **Classroom + mock exam** — `buildClassroomBatch`/`buildMockExam`/`scoreMockExam` gained `lang='bn'` param (default keeps `tools/batch_reference.mjs` + tests BYTE-IDENTICAL; the app passes the chosen lang); options/notes/prompts/section-titles/estimate localize. Answer-key stays deterministic (correct + distractors both `meaning.of(lang)`). classroomBatch/practiceBatch providers `ref.watch(langProvider)` → rebuild on switch. (4) **AI sensei** — `reply`/`explain`/`voiceReply` gained `uiLang`; authored EN + JA system + dict + voice prompts (correctness rule "never invent grammar" preserved in all 3); context wrapper, labels, offline fallbacks localized. **Scope boundary (honest):** the ~797 hardcoded UI-chrome strings (greeting "হাই", "AI ক্লাসরুম" label, buttons) and kana-recognition teaching (Bengali phonetics, no en/ja data) still show Bengali — that's the deferred translation+native-review program (options "Full parity" / "English only" not chosen). Verified: analyze clean · 71 tests · gate green · web rebuilt (locale=en persisted, Talk card shows English).

**D-042 | 2026-07-21 | "Talk with Sensei" — free, online-AI-led spoken practice that teaches sentence-building in sequence.** Owner: "make sure it's connected with online AI, anybody can talk with it, it teaches to talk like Japanese — making sentences, using words — all in one package, in sequence." The stack already existed (Home "সেনসেইয়ের সাথে কথা বলো / Talk with Sensei" card → `VoiceTutorScreen` → `voiceReply` → same-origin `/ai/chat` proxy, tier `teach`; STT + neural TTS + text fallback; offline-degrading). Improvements: (1) **Voice system prompts (3 langs) rebuilt** from casual chat → *guided sentence-building*: give ONE small target → model a short example → invite the learner to make their own → gently correct → build on prior (sequenced via the existing teaching contract); maxTokens 80→120 so a teach-example-then-invite turn isn't truncated. (2) **Opens by teaching** — `_greet()` now fires a live level-aware kickoff (not passive "ask me anything"); offline → localized canned invite. (3) **Text talk too** — `SenseiChatSheet` gained `openConversation` mode + `showTalkSheet()`; the Home sensei pill (previously a dead-end re-typing greeting) now opens it. (4) All talk UI chrome localized (bn/en/ja). Connectivity: code path confirmed + the proxy returned a real completion earlier this session when a provider key was present; needs a key in `.env` (D-041.5 loader) to answer live — offline it never blocks (D-001).

**D-043 | 2026-07-23 | On-device AI reachability — LAN dev proxy + cleartext (owner runs the proxy).** Symptom: on the phone the sensei said "offline" on WiFi, spoke Indian-English, and STT missed speech. Root causes: (1) the release APK's `AI_PROXY_URL` defaulted to the relative `/ai/chat` (works on web same-origin, meaningless on a device) → no reachable backend; (2) the proxy ran on the PC's localhost, unreachable from the phone; (3) `.env` had NO real keys — the values were the template's inline `# comments` (em-dash/space), which made `web_server` THROW on the Authorization header and crash on the first request (fixed c9d9c93: strip inline comments, reject non-ASCII keys, wrap `lib.request` in try/catch → fail over, never crash). Fix for device testing: `usesCleartextTraffic="true"` (allow http:// LAN) + rebuilt APK with `--dart-define=AI_PROXY_URL=http://192.168.8.141:5601/ai/chat`; proxy binds `*:5601` so the phone on the same WiFi reaches it (verified 200 on the LAN IP). REMAINING (owner): paste ONE real key into `.env` (one line, no comment) · run `node tools/web_server.mjs` in a terminal (keep open) · restore adb + reinstall `~/Downloads/bhasago.apk`. NOTE: LAN IP is baked into the APK (DHCP change → rebuild); "Indian English" = app UI set to English (Settings→Language→বাংলা for Bengali); STT needs the runtime mic permission granted. PROD: replace the LAN URL with an HTTPS-deployed proxy and drop cleartext.
